# SoundShield dependencies

# UI framework (LGPL, free for commercial use)
PySide6>=6.6.0

# Hugging Face transformers (install from source: pip install git+https://github.com/huggingface/transformers)
# transformers>=5.0.0

# Accelerate (required for device_map)
accelerate>=1.0.0

# PyTorch (CPU version)
torch>=2.0.0

# Audio processing
soundfile>=0.12.0
librosa>=0.10.0

# Utilities
numpy>=1.24.0
